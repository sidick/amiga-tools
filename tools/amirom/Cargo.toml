[package]
name = "amirom"
version = "0.1.0"
edition.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
description = "Inspect, scan, normalize, split and merge Amiga Kickstart ROM images"

[dependencies]
amiga-rom = { workspace = true }
clap = { version = "4", features = ["derive"] }
anyhow = "1"
